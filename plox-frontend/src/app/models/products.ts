import { NumberSymbol } from "@angular/common";

export class Products {
    prodId: number;
    prodName: string;
    prodDescription: string;
    prodPrice: number;
    createdAt: Date;
    updatedAt: Date;
}